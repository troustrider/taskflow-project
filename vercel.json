{
  "version": 2,
  "buildCommand": "npm run build:css",
  "outputDirectory": ".",
  "installCommand": "npm install && cd server && npm install",
  "functions": {
    "api/**/*.js": {
      "includeFiles": "server/**"
    }
  }
}
